# Hill & Hollow Coffee — Week 4 Starter Project

## Scenario

WebWorks Studio has received an approved visual direction for a small promotional landing page.

Your job is to implement that direction using **Tailwind CSS utility classes**.

## Important

The starter HTML is intentionally:

- semantic
- minimally styled
- responsive only after you add the appropriate Tailwind utilities

The Tailwind Play CDN script is already included in `index.html`.

## Before You Begin

1. Create:
   `webworks-studio/week04-tailwind/`
2. Place these starter files inside that folder.
3. Open the entire folder in Visual Studio Code.
4. Open `design-reference.svg` and review the supplied design direction.
5. Preview `index.html` in your browser.
6. Add Tailwind classes directly to the existing semantic HTML.

## Preserve

- Existing written content
- Semantic HTML structure
- Meaningful image alternative text
- Existing links and contact information

## Your Goal

Use Tailwind utilities to translate the supplied visual direction into a polished, responsive webpage.

## Tailwind Decisions

Before submitting, replace the placeholders below with approximately three examples.

### Decision 1

For the roast line-ups, I got very confused when I was trying to set the hover affect, I couldn't tell if the references wanted the background color or a shadow to appear. I originally went with the shadow. however changed it back to the background for it to appear closer to the reference.

### Decision 2

For the visit section, I really wanted to add it into the footer, since that's how it seemed to look in the reference, but I wasn't sure if I was suppose to change the file structure. so I tried to give it a similar appearance to the last module's visit section.

### Decision 3

I had a lot of trouble when dealing with images, I couldn't really get them to fit the way i intended, I ended up using flex to get the images in the proper spot but this still gave me trouble when trying to set and round behind it. so I settled for rounding the actual image. afterwards I set up the sm, and lg images because the images would shrink off the screen, it would downsize depending on size but continued to shrink as well, so I'm not sure I did it correctly. Any feed back would be helpful!

## Live Site

https://amandasgit.github.io/WebWorks-Studio/week04-tailwind/

## Reminder

The Tailwind Play CDN is being used for this instructional sprint only. It is intended for development, not production deployment workflows.

Refer to the Week 4 Canvas assignment for all requirements and submission directions.
